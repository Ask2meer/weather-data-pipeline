# weather-data-pipeline
A real-time data engineering pipeline using Apache Airflow, WeatherStack API, PostgreSQL, and Docker. The pipeline extracts live weather data, processes it, and loads it into a Postgres database automatically on a schedule.
